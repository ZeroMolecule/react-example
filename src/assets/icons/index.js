import symbolDefs from './symbol-defs.svg';

const icons = {
  angleDown: () => `${symbolDefs}#icon-angle-down`,
  plus: () => `${symbolDefs}#icon-plus`,
  search: () => `${symbolDefs}#icon-search`,
};

export default icons;
